# Catalogue migrations

Course seeds still live here (`001_add_stoicism.ts`). The runner is now general data migrations.

See `db/data-migrations/README.md`. App Runner executes pending migrations on start.
